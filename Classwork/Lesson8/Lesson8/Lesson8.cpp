#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Rus");

    int count;
    for (count = 0; count <= 5; count++)
    {
        cout << "Число: " << count << endl;
    }

}
